/*globals document, app */

// This code ties everything together after the DOM is ready
// and puts everything into a usable initial state
document.addEventListener('DOMContentLoaded', function () {
	'use strict';

	app.init();

});
